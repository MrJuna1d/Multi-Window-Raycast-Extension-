# Multi-Window Management

Manage Multiple windows and switch between them easily

## Limitation

This extension utilises the AppleScript instead of raycast official api to detect windows, why, because that api is only available to paid users.